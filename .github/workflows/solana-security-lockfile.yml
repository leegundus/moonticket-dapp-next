name: Solana security dependency refresh

on:
  push:
    branches:
      - solana-security-updates
    paths:
      - package.json
      - .github/workflows/solana-security-lockfile.yml

permissions:
  contents: write

jobs:
  refresh-lockfile:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Solana security branch
        uses: actions/checkout@v4
        with:
          ref: solana-security-updates

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Refresh package lock
        run: npm install --package-lock-only --ignore-scripts

      - name: Apply non-breaking audit fixes
        run: npm audit fix --package-lock-only --ignore-scripts || true

      - name: Report dependency audit
        run: npm audit || true

      - name: Commit refreshed lockfile
        run: |
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json is already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "Refresh lockfile for Solana security updates"
          git push origin solana-security-updates
